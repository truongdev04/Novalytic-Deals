# commands/ (legacy slash commands)

Empty by design. This project uses `.claude/skills/` for reusable
workflows instead of the older `commands/` mechanism. Add a command
here only if you specifically need the legacy slash-command format.
