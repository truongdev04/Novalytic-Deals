# Design rules

See "Design system" section in ../../CLAUDE.md for the source of truth
(colors, fonts, corners/shadows/motion).

This file is a placeholder for future detailed design rules that would
make CLAUDE.md too long (e.g. component-level spacing scale, breakpoint
conventions). Add specifics here only once CLAUDE.md's Design system
section stops being sufficient.
