# Workflow rules

See "Code conventions" and "Build, lint, test" sections in ../../CLAUDE.md.

Placeholder — add detailed workflow rules here (branching, review process,
commit conventions) only when CLAUDE.md becomes too long to hold them.
