# Tech defaults

See "Tech stack" section in ../../CLAUDE.md for the required/preferred
stack (Next.js App Router, TypeScript strict, Tailwind, shadcn/ui,
lucide-react, Framer Motion, React Hook Form + Zod).

Placeholder — add library-specific defaults here (e.g. preferred date
library, HTTP client conventions) only once they're decided.
